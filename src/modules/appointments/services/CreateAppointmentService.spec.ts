import CreateAppointmentService from './CreateAppointmentService';

describe('CreateAppointment', () => {
  it('should be able to create a new appointnent', () => {
    expect(1 + 2).toBe(3);
  });

  // it('should not be able to create two appointnents on the same time', () => {
  //   expect(1 + 2).toBe(3);
  // });
});
